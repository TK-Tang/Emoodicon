<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-4.1.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">



	<security:authentication-manager>
		<security:authentication-provider>
			<security:user-service>
				<security:user name="John" password="asdf"
					authorities="admin" />
				<security:user name="Zog" password="asdf" authorities="admin" />
			</security:user-service>
		</security:authentication-provider>
		<security:authentication-provider>
			<!-- dataSource is a data source bean connecting to a server -->
			<security:jdbc-user-service
				data-source-ref="dataSource" />
		</security:authentication-provider>
	</security:authentication-manager>

	<security:http use-expressions="true">
		<security:intercept-url pattern="/CreateOffer"
			access="isAuthenticated()" /> <!-- Don't typo the access = "exampleaccess" -->
		<security:intercept-url pattern="/doCreate"
			access="isAuthenticated()" />
		<security:intercept-url pattern="/OfferCreated"
			access="isAuthenticated()" />
		<security:intercept-url pattern="/NewAccount"
			access="permitAll" />
		<security:intercept-url pattern="/AccountCreated"
			access="permitAll" />
			<security:intercept-url pattern="/CreateAccount"
			access="permitAll" />
		<security:intercept-url pattern="/Error"
			access="permitAll" />
		<security:intercept-url pattern="/" access="permitAll" /> <!-- Spring works through these entries top to bottom -->
		<security:intercept-url pattern="/Offers"
			access="permitAll" />
		<security:intercept-url pattern="/static/**"
			access="permitAll" />
		<security:intercept-url pattern="/Login"
			access="permitAll" />
		<security:intercept-url pattern="/**" access="denyAll" />

		<security:form-login login-page="/Login"
			login-processing-url="/j_spring_security_check" username-parameter="j_username"
			password-parameter="j_password" authentication-failure-url="/Login?error=true" />
	</security:http>

</beans>
